function[lower, upper] = get_bound17(dim, cfunc_num)
switch cfunc_num
   
    case 1
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 2
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 3
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 4
        lower = ones(1, dim) * -10;
        upper = ones(1, dim) * 10;
        
    case 5
        lower = ones(1, dim) * -10;
        upper = ones(1, dim) * 10;
        
    case 6
        lower = ones(1, dim) * -20;
        upper = ones(1, dim) * 20;
    
    case 7
        lower = ones(1, dim) * -50;
        upper = ones(1, dim) * 50;
        
    case 8
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;      
        
    case 9
        lower = ones(1, dim) * -10;
        upper = ones(1, dim) * 10;
        
    case 10
       lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 11
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 12
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 13
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
   case 14
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
   case 15
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
    
    case 16
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
   case 17
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
   case 18
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
   case 19
        lower = ones(1, dim) * -50;
        upper = ones(1, dim) * 50;
        
    case 20
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 21
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 22
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 23
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 24
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 25
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 26
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 27
        lower = ones(1, dim) * -100;
        upper = ones(1, dim) * 100;
        
    case 28
        lower = ones(1, dim) * -50;
        upper = ones(1, dim) * 50;
        
end

end